import { NextResponse } from "next/server"; import { approveSolicitudHub, createSolicitudHub, getSolicitudesHub, rejectSolicitudHub } from "@/lib/data/solicitudes";
export async function GET() { return NextResponse.json(await getSolicitudesHub()); }
export async function POST(request: Request) { const body = await request.json(); if (body.action === "approve") return NextResponse.json(await approveSolicitudHub(body.id)); if (body.action === "reject") return NextResponse.json(await rejectSolicitudHub(body.id)); const required = ["nombre", "whatsapp", "email", "direccion", "hubSolicitadoId", "hubSolicitadoNombre", "servicio"]; if (required.some((key) => !String(body[key] || "").trim())) return NextResponse.json({ error: "Faltan campos obligatorios" }, { status: 400 }); return NextResponse.json(await createSolicitudHub(body), { status: 201 }); }
