import { HubNav } from "../HubNav";
import { getHubOr404 } from "../utils";
const grupos = ["Notificar a todos", "Solo clientes", "Solo oferentes activos", "Solo postulantes", "Solo paisajistas", "Solo viveros"];
export default async function NotificacionesPage({ params }: { params: Promise<{ hubId: string }> }) { const { hubId } = await params; const hub = await getHubOr404(hubId); return <main className="min-h-screen bg-[#f6f7f2] px-4 py-6 text-[#1f2a1d]"><div className="mx-auto grid max-w-7xl gap-5"><HubNav hub={hub} active="notificaciones" /><section className="rounded-[2rem] border border-[#d8dfd1] bg-white p-6 shadow-sm"><h2 className="text-2xl font-black">Notificaciones</h2><p className="mt-2 text-sm font-bold text-[#66745c]">Todos los integrantes de la ficha deben tener email antes del envío.</p><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{grupos.map((g) => <button key={g} className="rounded-2xl border border-[#cfd8c6] bg-[#f8faf5] px-4 py-5 text-left font-black">{g}</button>)}</div><textarea className="mt-6 min-h-40 w-full rounded-2xl border p-4" placeholder="Mensaje para la ficha del Hub" /><button className="mt-4 rounded-xl bg-[#1f2a1d] px-5 py-3 font-black text-white">Enviar mails</button></section></div></main>; }
