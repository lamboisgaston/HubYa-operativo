import { HubNav } from "../HubNav";
import { getHubOr404 } from "../utils";
export default async function VinculosPage({ params }: { params: Promise<{ hubId: string }> }) { const { hubId } = await params; const hub = await getHubOr404(hubId); return <main className="min-h-screen bg-[#f6f7f2] px-4 py-6 text-[#1f2a1d]"><div className="mx-auto grid max-w-7xl gap-5"><HubNav hub={hub} active="vinculos" /><section className="rounded-[2rem] border border-[#d8dfd1] bg-white p-6 shadow-sm"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-2xl font-black">Vínculos / servicios</h2><p className="text-sm font-bold text-[#66745c]">Administra quién abastece cada servicio del Hub.</p></div><div className="flex gap-2"><button className="rounded-xl bg-[#1f2a1d] px-4 py-3 font-black text-white">Agregar servicio</button><button className="rounded-xl border px-4 py-3 font-black">Agregar oferente</button></div></div><div className="mt-6 grid gap-4">{hub.servicios.map((servicio) => <article key={servicio.id} className="rounded-2xl bg-[#f8faf5] p-5"><h3 className="text-lg font-black">Servicio: {servicio.nombre_servicio}</h3><p className="mt-2 font-bold">Activo: {servicio.vinculoActivo?.oferta_nombre || "Sin activo"}</p><p className="text-sm text-[#66745c]">Postulantes: {servicio.postulantes.map((p) => p.oferta_nombre).join(", ") || "Sin postulantes"}</p><div className="mt-4 flex flex-wrap gap-2"><button className="rounded-xl border px-3 py-2 text-xs font-black">Remover</button><button className="rounded-xl border px-3 py-2 text-xs font-black">Aprobar postulante</button><button className="rounded-xl border px-3 py-2 text-xs font-black">Ver perfil</button></div></article>)}</div></section></div></main>; }
