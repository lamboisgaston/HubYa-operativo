import { getHubs } from "@/lib/data/hubs";
export default async function PostulantesPage() { const hubs = await getHubs(); const rows = hubs.flatMap((h) => h.servicios.flatMap((s) => s.postulantes.map((p) => ({ hub:h.nombre, servicio:s.nombre_servicio, ...p })))); return <main className="min-h-screen bg-[#f6f7f2] px-4 py-6 text-[#1f2a1d]"><section className="mx-auto max-w-7xl rounded-[2rem] border border-[#d8dfd1] bg-white p-6 shadow-sm"><a href="/operativo" className="text-xs font-black uppercase tracking-[0.2em] text-[#7a8a6d]">← Mis Hubs</a><h1 className="mt-3 text-3xl font-black">Postulantes</h1><div className="mt-6 overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="bg-[#f1f4ec] text-xs uppercase text-[#66745c]"><tr>{["Nombre","Servicio","Hub al que se postula","Email","Teléfono","Estado","Acciones"].map(h=><th key={h} className="p-3">{h}</th>)}</tr></thead><tbody>{rows.map((r)=><tr key={r.id} className="border-t"><td className="p-3 font-black">{r.oferta_nombre}</td><td className="p-3">{r.servicio}</td><td className="p-3">{r.hub}</td><td className="p-3">—</td><td className="p-3">—</td><td className="p-3">{r.estado}</td><td className="p-3"><div className="flex gap-2"><button>Ver perfil</button><button>Aprobar</button><button>Rechazar</button></div></td></tr>)}</tbody></table></div></section></main>; }
