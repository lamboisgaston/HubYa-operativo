import { getHubs } from "@/lib/data/hubs";
export default async function PerfilesPage() { const hubs = await getHubs(); const perfiles = hubs.flatMap((h)=>h.servicios.flatMap((s)=>[s.vinculoActivo, ...s.postulantes].filter(Boolean).map((p)=>({hub:h.nombre, servicio:s.nombre_servicio, ...p!})))); return <main className="min-h-screen bg-[#f6f7f2] px-4 py-6 text-[#1f2a1d]"><section className="mx-auto max-w-7xl rounded-[2rem] border border-[#d8dfd1] bg-white p-6 shadow-sm"><a href="/operativo" className="text-xs font-black uppercase tracking-[0.2em] text-[#7a8a6d]">← Mis Hubs</a><h1 className="mt-3 text-3xl font-black">Perfiles</h1><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{perfiles.map((p)=><article key={p.id} className="rounded-2xl bg-[#f8faf5] p-5"><h2 className="font-black">{p.oferta_nombre}</h2><p className="text-sm">Rol: {p.responsable || "Oferente"}</p><p className="text-sm">Servicio: {p.servicio}</p><p className="text-sm">Email: — · WhatsApp: —</p><p className="text-sm">Hubs donde participa: {p.hub}</p><p className="text-sm">Estado: {p.estado}</p><p className="mt-2 text-sm text-[#66745c]">Observaciones: {p.observaciones}</p></article>)}</div></section></main>; }
